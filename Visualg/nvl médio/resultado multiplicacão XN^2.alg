algoritmo "resultado de x.n^2"
// Função :
// Autor :
// Data : 10/01/2019
// Seção de Declarações 
var
x,n,resultado: real

inicio
escreval("calculo da operação (X*N) elevado ao quadrado")
escreval("digite um valor para X")
leia(x)
escreval("digite um valor para N")
leia(n)
resultado<-Exp(x*n,2)
escreval("o resultado de (x*n) elevado ao quadrado é",resultado)

// Seção de Comandos 
fimalgoritmo
